"""Validator for the `neuron_property_filter` block UI element."""


def validate_neuron_property_filter(schema: dict, param: str, ref: str) -> None:
    """Validate a `neuron_property_filter` field.

    Validation is not yet implemented for this UI element; the function is a
    registered no-op so the dispatcher has a home for the element.
    """
